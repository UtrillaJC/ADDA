
public class MatrizEquisuma {
	
	/* Trabajo con una matriz de 2 x 2 
	 * Hay que detectar que la N es potencia de 2 
	 *            / d[i][j] + d[i][j+1] + d[]             N = 2
	 *   mEq() = |
	 */
	public Integer matrizEquisuma(int i, int j, int pot2, int[][] matriz){
		Integer ret = 0;
		
		if()
		
		return ret;
	}
	
	public static void main(String[] args) {
		
	}
}
